# Build output (objects, deps, file_ids.mk, auto_file_ids.h, binary)
output/

# Simulation external storage + dump artifacts
sim_data/
ram_dump.*
storage_dump.*
log.hex
log.txt

# Note: ww_log_map.json is generated but COMMITTED (drives build + decode).
#       output/auto_file_ids.h is derived by `make` from the map.
